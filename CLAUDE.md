# my-agent プロジェクトルール

## プロジェクト概要
個人PM管理・ナレッジベースシステム。複数のプロジェクト（PJ）を管理し、各プロジェクトのドキュメントをMDX形式で管理するNext.jsアプリケーション。

## タスク着手前の必須確認（最重要）

**タスクに着手する前に、必ず以下を実行すること：**

1. **全体の状況を確認する**
   - 関連するファイルやコードを読んで現状を把握する
   - 既存の実装パターンや構造を理解する
   - 依存関係や影響範囲を確認する

2. **脳死で作業しない**
   - 何も確認せずにいきなりコードを書き始めない
   - ユーザーの意図と現状の差分を理解してから作業する
   - 不明点があれば確認する

3. **確認すべきこと**
   - 変更対象のファイルの現在の内容
   - 類似の実装がすでに存在しないか
   - プロジェクトの慣習やパターン

**このルールを守らずに作業を始めることは禁止**

## アーキテクチャ

### ディレクトリ構造
- `app/`: Next.js App Router（ページとレイアウト）
  - `[pj]/`: プロジェクト別の動的ルーティング
  - `layout.tsx`: ルートレイアウト（認証含む）
  - `page.tsx`: ホームページ（ログイン画面 or プロジェクト一覧）
- `components/`: Reactコンポーネント
  - `PJSidebar.tsx`: プロジェクトサイドバーナビゲーション
  - `MDXContent.tsx`: MDXコンテンツレンダラー
- `lib/`: ビジネスロジック
  - `mdx.ts`: MDXファイル読み込み・ナビゲーション生成
- `content/`: プロジェクトコンテンツ
  - `{pj-id}/`: プロジェクトディレクトリ
    - `_pj.json`: プロジェクトメタデータ
    - `*.mdx`: MDXコンテンツファイル

### ルーティング
- `/`: ホームページ（ログイン or プロジェクト一覧）
- `/[pj]`: プロジェクトトップページ
- `/[pj]/[...slug]`: MDXページの動的ルーティング

### 認証
- パスワード認証（デフォルト: `9230`）
- 24時間有効なトークンベースセッション
- `app/layout.tsx` に認証ロジックが集約

## コーディングルール

### 命名規則
- コンポーネント: PascalCase（`PJSidebar.tsx`）
- 関数: camelCase（`getPJs()`）
- 型: PascalCase（`PJMeta`）
- ディレクトリ: kebab-case（`my-meviy-pj`）

### コンポーネント設計
- **Server Componentsをデフォルト**: クライアント側の状態管理が必要な場合のみ `"use client"` を使用
- データ取得はServer Componentsで実行
- 認証状態は `AuthProvider` で管理

### 型定義
- `lib/mdx.ts` に主要な型定義を集約
  - `PJMeta`: プロジェクトメタデータ
  - `MDXPage`: MDXページデータ
  - `NavItem`: ナビゲーションアイテム

### スタイリング
- Tailwind CSSを使用
- 白背景（`bg-white`）をデフォルト
- グレースケールのカラーパレット

## 重要なファイル

### `lib/mdx.ts`
- `getPJs()`: プロジェクト一覧を取得
- `getPJNavTree(pjId)`: プロジェクトのナビゲーションツリーを生成
- `getMDXPage(slugs)`: MDXページを取得（URLデコード対応）

### `app/[pj]/layout.tsx`
- プロジェクトレイアウト（サイドバー付き）
- プロジェクトメタデータの表示

### `components/PJSidebar.tsx`
- プロジェクトのナビゲーションサイドバー
- コメントアウトされた項目に取り消し線を表示

## MDXファイル

### フロントマター
```yaml
---
title: ページタイトル
description: 説明
tags: [タグ1, タグ2]
date: YYYY-MM-DD
---
```

### 注意事項
- HTMLコメント `<!-- -->` は使用不可。MDXコメント `{/* */}` を使用
- 日本語を含むパスは自動的にURLエンコード/デコードされる

## プロジェクト追加方法

1. `content/{pj-id}/` ディレクトリを作成
2. `_pj.json` ファイルを作成（メタデータ）
3. MDXファイルを追加

## よくあるタスク

### 新しいプロジェクトを追加
1. `content/` にディレクトリ作成
2. `_pj.json` を作成
3. MDXファイルを追加

### ページをコメントアウト
- MDXファイルの本文を `{/* */}` で囲む
- サイドバーには表示されるが、内容は非表示

### 認証パスワードを変更
- `app/layout.tsx` の `loginAction` 関数内の `PASSWORD` を変更

## 技術スタック
- Next.js 16 (App Router)
- TypeScript
- Tailwind CSS
- MDX (next-mdx-remote)
- gray-matter
